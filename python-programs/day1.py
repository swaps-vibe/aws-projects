def sum_numbers(numbers):
     total = 0
     for num in numbers:
        total=total+num
    
        print( f"sum is :{total}" )
        return total

numbers = [10,20,30,40] 
    
sum_numbers(numbers)   
    